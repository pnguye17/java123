package collectionsTutorial;

public class mapTutorial {

	public static void main(String[] args) {
		//hashmap - can have null k/v, not thread safe, not ordered, 
		//treemap - natural order by keys, not thread safe

	}

}
